// This component is superseded by the inline media grid in the IndexPage.
// Kept as an empty stub to avoid breaking the build if referenced anywhere.
import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-file-grid',
  standalone: true,
  imports: [CommonModule],
  template: ''
})
export class FileGridComponent {}
